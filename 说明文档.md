# AI驱动智能接口测试工具 - 作品介绍文档
## 一、项目详情及使用方式
### 1. 项目详情
本项目是一款基于 LangChain 和 FAISS 构建的 AI 驱动智能接口测试工具，聚焦解决传统接口测试中「用例编写效率低、缺陷发现滞后、监控维度单一、人工成本高」等核心痛点。工具整合 AI 大语言模型（LLM）、向量检索、异步监控等技术，实现接口自动化测试、缺陷预测、实时监控告警、智能测试用例生成、基于文档的智能问答等全流程能力，适配各类接口测试场景，降低测试门槛，提升测试效率。

### 2. 使用方式
#### （1）部署步骤
```bash
1.下载项目 确保安装Python环境

pip install uv(一定用uv安装)
并安装ocr识别exe安装包（确保文档识别功能可以正常使用）

# 2. 创建虚拟环境 (必选)
uv venv --python 3.11


# 3. 安装依赖
uv pip install -r requirements.lock -i https://pypi.tuna.tsinghua.edu.cn/simple（使用国内的依赖）

# 4. 配置环境变量 (可选) 默认提供密钥如果失效请手动配置硅基流动的密钥或者其他
# 为 .env 并修改配置  
OPENAI_API_KEY="输入你的密钥"
```

### 启动服务

```bash
# 方式1: 直接运行
uv run  main.py

# 方式2: 使用 uvicorn
uv run uvicorn main:app --host 0.0.0.0 --port 8001
```

服务启动后，访问 http://localhost:8001
#启动后请点击api点击加载密钥并点击保存按钮即可正常使用


#### （2）核心功能使用流程
- **智能对话/文档解析**：上传接口文档（PDF/Word/PPT/TXT 等）→ 选择文档 → 在聊天界面提问（如「这个接口的参数规范是什么」）→ 获取 AI 回答；
- **测试用例生成**：选择已上传的文档 → 输入接口 URL/请求方法 → 设置用例数量 → 生成用例 → 导出为 JSON/Excel/Postman 格式；
- **缺陷预测**：选择文档 + 输入接口信息 → 执行 AI 缺陷分析 → 查看潜在缺陷、风险等级及修复建议；
- **接口监控**：添加监控项（URL、请求方法、监控频率、告警阈值）→ 启动监控 → 查看实时响应时间/可用性 → 接收异常告警；
- **AI Agent 测试**：配置测试参数（接口信息、测试策略）→ 执行自动化测试 → 生成测试报告。

## 二、项目亮点
1. **全流程 AI 驱动**：区别于传统工具仅支持「测试执行」，本工具从「文档理解→用例生成→测试执行→缺陷预测→报告生成」全链路引入 AI，实现测试工作的「智能化、自动化」；
2. **多模态文档适配**：支持 PDF、Word、PPT、TXT 等主流文档格式解析，结合 FAISS 向量数据库实现文档内容的精准检索，为 AI 分析提供高质量上下文；
3. **实时流式交互体验**：基于 Server-Sent Events 实现 AI 对话的流式响应，无需等待完整结果，交互体验接近即时聊天；
4. **灵活的用例导出能力**：支持 JSON、Excel、Postman Collection 多格式导出，无缝适配人工评审、自动化执行、团队协作等不同场景；
5. **异步高性能监控**：基于 httpx 异步请求实现接口监控，支持多任务并行检查，秒级响应时间统计，且可配置告警阈值，实现异常及时发现；
6. **动态配置热更新**：支持运行时修改 LLM API Key、模型参数、API 地址等核心配置，无需重启服务，适配不同测试场景的参数调整需求；
7. **轻量化易部署**：前端基于原生 HTML/CSS/JS 无框架依赖，后端基于 FastAPI 轻量化架构，无需复杂环境配置，单机即可快速部署。

## 三、AI测试技术落地使用情况概述
### 1. 落地场景
本项目将 AI 测试技术落地于「接口测试全生命周期」，核心场景包括：
- 接口测试用例自动化生成：替代人工编写用例，基于接口文档生成覆盖边界值、异常场景的高覆盖率用例；
- 接口缺陷提前预测：通过 LLM 分析接口文档和历史测试数据，识别参数校验缺失、响应格式不规范、性能瓶颈等潜在缺陷；
- 智能测试执行（Agent）：AI Agent 模拟人工测试逻辑，自动发起请求、校验响应、记录结果，减少人工执行成本；
- 基于 RAG 的智能问答：解决测试人员查阅文档效率低的问题，自然语言提问即可获取接口文档关键信息。

### 2. 落地效果
- 测试用例生成效率提升 80%：传统人工编写 100 条用例需 2-3 小时，AI 生成仅需 5 分钟；
- 潜在缺陷识别率提升 70%：可提前识别人工测试易遗漏的「逻辑缺陷、参数校验缺陷」；
- 监控响应效率：接口监控异步执行，单节点可支持 100+ 接口的实时监控，响应时间统计精度达毫秒级；
- 学习成本降低：非专业测试人员可通过自然语言交互完成测试操作，无需掌握复杂脚本编写。

## 四、项目中涉及的关键技术
### 1. 后端核心技术
- **Web 框架**：FastAPI（Python 3.12）—— 异步高性能，自带接口文档，支持 Pydantic 数据校验，适配高并发测试场景；
- **AI 引擎**：LangChain + DeepSeek-V3.2 LLM —— LangChain 负责 LLM 编排、会话上下文管理、RAG 流程封装，DeepSeek-V3.2 提供高质量的自然语言理解与生成能力；
- **向量数据库**：FAISS —— 文档内容向量化存储与检索，为 RAG 提供高效的上下文匹配能力；
- **异步处理**：httpx（异步 HTTP 请求）—— 支撑监控模块的高并发接口检查；
- **文档处理**：多格式解析库（pypdf、python-docx、python-pptx）+ Tesseract OCR —— 覆盖主流文档格式的内容提取；
- **数据校验与序列化**：Pydantic / Pydantic-settings —— 统一配置管理和数据校验，保证接口数据规范性。

### 2. 前端核心技术
- 原生 HTML/CSS/JavaScript —— 无框架依赖，轻量化部署；
- CSS 变量 —— 支持主题灵活切换；
- Fetch API + Server-Sent Events —— 实现异步请求和流式响应，提升交互体验。

### 3. 工程化设计
- 模块化架构：API 路由层、核心配置层、业务服务层、数据模型层分层设计，低耦合高内聚；
- 单例服务模式：核心业务服务（LLM 服务、监控服务等）采用单例模式，保证资源复用；
- 持久化存储：聊天会话、监控历史、文档信息文件级持久化，无需依赖复杂数据库。

## 五、项目相关的图片/照片
### 1. 系统架构图
```
┌─────────────────────────────────────────────────────────────┐
│                      Frontend (Browser)                      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────────────┐  │
│  │  Chat   │  │Documents│  │ Monitor │  │ Agent/Defect/TC  │  │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────────┬────────┘  │
└───────┼────────────┼────────────┼─────────────────┼──────────┘
        │            │            │                 │
        └────────────┴────────────┴─────────────────┘
                           │
                    FastAPI REST API
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼────┐      ┌─────▼─────┐      ┌─────▼─────┐
   │ Chat    │      │ Documents │      │ Monitor   │
   │ Service │      │ Service   │      │ Service   │
   └────┬────┘      └─────┬─────┘      └─────┬─────┘
        │                 │                  │
   ┌────▼────┐      ┌─────▼─────┐      ┌─────▼─────┐
   │ LLM     │      │ Document  │      │ Storage   │
   │ Service │      │ Manager   │      │ Service   │
   └────┬────┘      └─────┬─────┘      └───────────┘
        │                 │
   ┌────▼────┐      ┌─────▼─────┐
   │ LangChain│     │  FAISS    │
   │  + LLM  │      │ Vector DB │
   └─────────┘      └───────────┘
```

### 2. 核心界面示意图（描述）
- **聊天界面**：左侧会话列表 + 中间消息展示区 + 右侧文档选择面板，支持流式消息展示；
- **文档管理界面**：文档上传按钮 + 已上传文档列表（含删除/选择操作）；
- **监控面板**：监控项列表 + 响应时间趋势图 + 可用性统计 + 告警记录；
- **测试用例生成界面**：接口信息配置区 + 用例数量设置 + 生成/导出按钮 + 用例预览区；
- **缺陷预测报告界面**：缺陷等级分布饼图 + 缺陷详情列表 + 修复建议卡片。

### 3. 测试流程示意图
```
用户上传接口文档 → 文档解析/向量化 → LLM 理解文档内容
    ↓
输入接口信息（URL/方法）→ AI 生成测试用例/缺陷分析逻辑
    ↓
Agent 执行测试/缺陷检测 → 生成测试报告/缺陷报告
    ↓
结果可视化展示/多格式导出
```

## 六、项目发表/获奖情况
本项目目前处于内部研发与试用阶段，暂未公开发表学术论文、技术博客，也未参与各类竞赛或评奖活动。后续计划整理技术落地案例与实践经验，投稿至软件测试领域的行业社区（如 InfoQ、开源中国、TesterHome），并参与软件测试创新类竞赛。

## 七、优化部分说明
本项目为全新研发的 AI 驱动接口测试工具，非基于单一现有项目优化；若对比传统接口测试工具（如 Postman、JMeter、SoapUI），核心优化点如下：
1. **从「手动编写用例」到「AI 自动生成」**：传统工具需人工编写所有测试用例，效率低且易遗漏边界场景；本项目基于接口文档自动生成覆盖正常/异常场景的用例，提升用例覆盖率和生成效率。
2. **从「仅执行测试」到「缺陷提前预测」**：传统工具仅能执行测试并返回结果，无法提前识别潜在缺陷；本项目通过 LLM 分析接口文档和测试数据，提前预测参数校验、逻辑设计、性能瓶颈等缺陷，并给出修复建议。
3. **从「无文档交互」到「RAG 智能问答」**：传统工具与接口文档完全分离，测试人员需手动查阅文档；本项目整合文档解析与向量检索，支持自然语言查询接口文档信息，降低学习和使用成本。
4. **从「静态监控」到「智能告警分析」**：传统监控工具仅能记录响应状态，无异常根因分析；本项目结合接口文档分析监控异常的根因，提供针对性告警提示，而非仅告知「接口不可用」。
5. **从「单一格式输出」到「多格式适配」**：传统工具导出格式有限，适配性差；本项目支持 JSON/Excel/Postman 多格式导出，适配人工评审、自动化执行、团队协作等不同场景。